import 'package:flutter/material.dart';

class varaprasad extends StatelessWidget {
  const varaprasad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("hi! varaprasad "),
      ),
    );
  }
}
